## What

Brief description of the change.

## Why

Motivation / linked issue.

## Checks

- [ ] `npm run typecheck` passes in the touched package(s)
- [ ] `cargo odra test` passes (if `contracts/` changed)
- [ ] No secrets or key material committed
- [ ] `main` stays deployable (app is served from `main`)
