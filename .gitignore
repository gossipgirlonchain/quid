# node (node_modules* also catches iCloud conflict-renames like "node_modules 2")
node_modules*
dist/
.env
.env.*
!.env.example

# rust / odra (target is a *.nosync symlink locally - see the checklist's environment note)
target
target.nosync/
*.nosync
Cargo.lock
*.wasm

# keys, never commit
keys/
*.pem

# vercel
.vercel
.claude/

# os
.DS_Store
*.log
